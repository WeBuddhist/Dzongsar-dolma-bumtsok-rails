Read `4-SYSTEM/Skills/outline-extract/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Extract the structural outline (ས་བཅད། / sa bcad) already embedded in a Tibetan commentary and emit it as a standalone nested file — YAML frontmatter, heading-based hierarchy for levels 1–5 (`# ` title, `## ` level-1 … `###### ` level-5), indented bold list items deeper than that.
