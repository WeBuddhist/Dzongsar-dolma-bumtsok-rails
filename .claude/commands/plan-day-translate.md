Read `4-SYSTEM/Skills/plan-day-translate/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Produce a translation stream's day file by translating the authoring stream's day file section by section — locating each section by normalised heading text (never by number or position), retrieving verse text by block ID from that stream's own designated verse translation, obeying the stream's `termbase-translation.md` fork and its pending-terms loop, then auditing the result back against the source with fresh eyes.
