Read `4-SYSTEM/Skills/root-text-upload/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Lint, parse and upload one root text to the library backend as a text, an edition whose segmentation references are its block IDs, and a table of contents. The root is published before its translations, which need its text_id and edition_id.
