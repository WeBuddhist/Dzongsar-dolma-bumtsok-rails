Read `4-SYSTEM/Skills/bilingual-to-transclusion/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Convert a translation that carries the root text inline (root, separator, translation per block) into transclusion form, after checking every inline root block against the root.
