Read `4-SYSTEM/Skills/extract-source-metadata/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Pull a source text's own metadata — title, author or composer, translator, publication details, scribe, patron, place and date — out of its first and last pages, and write it into the file's frontmatter properties.
