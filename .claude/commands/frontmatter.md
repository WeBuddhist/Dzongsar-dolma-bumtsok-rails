Read `4-SYSTEM/Skills/frontmatter/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Populate the complete YAML frontmatter for any text file in the collection — root text, commentary, translation, or reference — by extracting the metadata from the file's own title, colophon, and opening lines and writing the block that matches that file type's schema.
