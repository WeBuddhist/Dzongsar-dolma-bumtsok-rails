Read `4-SYSTEM/Skills/toc-generate/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Build a Tibetan Buddhist text's ས་བཅད (sa bcad) table of contents end to end — scan the text for structural-outline candidates, copy the division-announcement passages verbatim, reconcile them into one nested decimal tree, verify that tree against the source with two deterministic checkers, and ingest it back into the text as markdown headings with block IDs.
