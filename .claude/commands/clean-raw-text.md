Read `4-SYSTEM/Skills/clean-raw-text/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Inspect a raw text for mechanical damage — page markers, running headers and footers, OCR index numbers, stray spacing, encoding artifacts — generate a targeted cleaning script for exactly what that file has, run it, and save the cleaned draft.
