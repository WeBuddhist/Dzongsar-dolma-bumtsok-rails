Read `3-SKILLS/commentary-upload/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Lint, parse and upload one commentary to the library backend as a commentary of the root text: text, edition, table of contents, and an alignment from its segments to the root edition. Footnotes are excluded from every payload. Dry-run by default.
