Read `4-SYSTEM/Skills/epub-to-markdown/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Convert an EPUB into structured markdown, adaptively: inspect the epub's own internal structure first, then either reuse an existing publisher-specific converter or generate a new one tailored to that epub's conventions.
