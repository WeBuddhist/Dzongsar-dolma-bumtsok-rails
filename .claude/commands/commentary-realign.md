Read `3-SKILLS/commentary-realign/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Copy a commentary aligned to one root text and re-align the copy to a second root text that contains the same root passages (repeated passages get every occurrence), carrying the existing alignment across, then delete the old file.
