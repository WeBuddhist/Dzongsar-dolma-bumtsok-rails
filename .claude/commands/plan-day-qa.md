Read `4-SYSTEM/Skills/plan-day-qa/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Grade one plan day file against the plan's own contracts — structure and fidelity against the section declaration in `About <plan-name>.md`, grounding against the rails (authoring stream) or the authoring-stream day file (translation streams), voice and style against `<lang>/requirements.md`, and the declared limits — producing a scorecard with a severity per failure, the offending text quoted, a one-line fix, a computed score out of 10 and a hard verdict gate.
