Read `4-SYSTEM/Skills/plan-day-package/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Build the machine-anchored per-day dossier that an app or API consumes: a source-of-record package that copies the day's verse rails verbatim, plus its translation, each carrying the plan's challenge sections, the day's verses by block ID, and every rail layer under `<!-- … -->` anchors with display-only headings.
