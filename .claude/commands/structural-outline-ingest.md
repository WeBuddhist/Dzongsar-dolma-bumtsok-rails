Read `4-SYSTEM/Skills/structural-outline-ingest/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Extract the structural outline of one source (root text, commentary, translation or reference) and write it as a citable rail at $SECTIONS_RAW/outline/<source-id>.md.
