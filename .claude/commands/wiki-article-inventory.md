Read `4-SYSTEM/Skills/wiki-article-inventory/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Determine, for every standalone article subject, whether the target Wikipedia (bo.wikipedia in the worked example) already has the article — via title/variant lookup and Wikidata sitelinks — and save a per-subject inventory with dated wikitext snapshots plus a minimal publishing registry.
