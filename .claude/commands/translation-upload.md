Read `4-SYSTEM/Skills/translation-upload/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Lint, parse and upload one finished translation to the library backend as an edition of its own text — aligned segment-for-segment to the published root edition and carrying its own table of contents.
