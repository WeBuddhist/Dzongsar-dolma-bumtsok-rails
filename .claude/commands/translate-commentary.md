Read `4-SYSTEM/Skills/translate-commentary/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Translate a commentary into a target language against a translation track's requirements, termbase and section rails, preserving every block ID and heading so the output stays block-aligned with the source.
