Read `4-SYSTEM/Skills/yigchung-upload/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Parse the `<small>…</small>` yigchung marks out of an uploaded note and attach them to its live edition as yigchung annotations, one tag-free span each.
