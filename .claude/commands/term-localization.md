Read `4-SYSTEM/Skills/term-localization/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Translate a text's key terms in the term-localization table into the track's target languages, deriving each rendering from the commentary-based Meaning column rather than generic dictionary lookups.
