Read `4-SYSTEM/Skills/bilingual-glossary/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Build and maintain a project's bilingual glossary end to end: extract every source-language keyword and its attested renderings from each (source, translation) pair, merge the per-source files into one consolidated glossary per language pair, flag the terms whose renderings genuinely disagree, and select the preferred rendering for a particular translation track.
