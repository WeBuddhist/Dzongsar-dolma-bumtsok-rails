Read `4-SYSTEM/Skills/spine-map/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Build one commentary's routing index from its own TOC nodes and claim IDs onto the canonical spine slots of the root text — the once-per-commentary judgment that lets claims-consolidation assemble any topic's packet by script instead of re-reading every raw claims file per topic.
