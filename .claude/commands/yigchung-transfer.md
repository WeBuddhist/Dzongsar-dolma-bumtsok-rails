Read `4-SYSTEM/Skills/yigchung-transfer/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Carry the root's `<small>…</small>` yigchung marks over to a block-aligned translation — whole-block marks mechanically, partial ones by meaning from a worklist.
